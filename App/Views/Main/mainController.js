/**
 * @classdesc Application app.js main controller.
 */
(function() {
    var app = angular.module("secApp");

    var mainController=function($scope,$log,customModuls){

    };

    app.controller("mainController",mainController);
    
}());