/**
 * @classdesc Application app.js main service that responsible for main in-out data flow.
 */
(function() {
    var module = angular.module("mainService");
    var mainService=function($http){


        
    };

    module.controller("mainService",mainService);
}());